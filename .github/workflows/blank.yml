on:
  push:
    branches:
      - main

name: dashboard-api-github-actions-flow

jobs:
  dashboard_api_build_and_push_to_ECR:
    name: build and push to ECR dashboard_api
    runs-on: ubuntu-latest

    steps:         
      - uses: actions/checkout@v3         
      - name: echo 
        uses: ./gunahk/github-dirty-hands-on/.github/workflows/test.yml
        with: 
          config-path: ${{ secrets.SECRET2 }}
          secrets: ${{ secrets.SECRET1 }}